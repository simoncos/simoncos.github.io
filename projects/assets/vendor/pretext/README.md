# Pretext browser assets

These files vendor the runtime subset of
[`@chenglou/pretext`](https://github.com/chenglou/pretext) version `0.0.8`
needed by the Sleep Essay variable-lens interaction.

They are committed because the site is published as static files and does not
serve `node_modules`. The upstream package is MIT-licensed; see `LICENSE`.
